/**
 *  @file main.cpp
 *  @author What's your name, huh?
 *  @date What day and section?
 */

#include <iostream>
#include <string>
using namespace std;

#include "cs150check.h"
// Header file for problem being tested

/**
 * Run your program's tests
 */
void runTests()
{
    ///////////// Begin a set of tests
    beginTests(); // test heading

    /////// Tests for ????? //////////////////////
    beginFunctionTest("Put the function name here.");

    // Use any of these test functions
    // assertEquals(expected, actual);
    // assertEqualsMsg(message, expected, actual);
    // assertDoubleEquals(expected, actual, tolerance);
    // assertDoubleEqualsMsg(message, expected, actual, tolerance);
    // assertTrue(Boolean expression)
    // assertFalse(Boolean expression)
    // assertTrueMsg(Boolean expression, message)
    // assertFalseMsg(Boolean expression, message)
    // assertArrayEquals(expected, actual, number of elements)
    // assertArrayEqualsMsg(message, expected, actual, number of elements)

    endFunctionTest(); // end

    endTests();
}

